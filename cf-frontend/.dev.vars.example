# 复制为 .dev.vars（gitignored）并填入真实值，供 `wrangler pages dev` 本地调试。
# 生产环境请用 `wrangler pages secret put <名> --project-name <项目>` 配置。

PRENIV_API_TOKEN=48位hex（仓库 .env 里的 PRENIV_API_TOKEN 值）
CAPTCHA_SECRET=64位hex随机（验证码/会话的 HMAC 密钥，本地随便生成一个）
# 未配置 CAPTCHA_SECRET 时：验证码接口 503、API 完全开放（仅限本地开发）

# 可选：联调本地后端（带 cookies 的 8787 或模拟云端的 8789）
# PRENIV_API_BASE=http://127.0.0.1:8787